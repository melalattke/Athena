<h1>Welcome lalala</h1>
<%if !user_signed_in? %>
<%= link_to('register', new_user_session_test_path) %>
<%end%>

<table>
  <thead>
    <tr>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>

        <td><%= link_to item.name + " (" + number_to_currency(item.price) + ")", item %></td>

      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'New Item', new_item_path %>
